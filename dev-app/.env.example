# Comma-separated secret keys to preload into the Select Merchant picker.
# Also used in CI to inject test accounts. Requires Metro cache clear after
# changes (e.g. `yarn start --reset-cache`).
PRELOADED_SECRET_KEYS=